{% extends '@default/layout.twig' %}

{% block body %}
    <h1>{{ theme_name }}</h1>
    <p>TODO: this will be a form</p>
    {% for key, value in settings %}
        <h2>{{ key|replace({'_': ' '})|title }}</h2>
        <p>{{ value }}</p>
    {% endfor %}
    <form action="{{ path('post_theme_settings') }}" method="post">
        {{ form_widget(form) }}

        <input type="submit" name="submit" />
    </form>
{% endblock %}